// Import the plugins
import tailwindcss from 'tailwindcss';
import autoprefixer from 'autoprefixer';

// Export the configuration
export default {
  plugins: [
    tailwindcss,
    autoprefixer,
    //postcssRtl({})
  ]
};
